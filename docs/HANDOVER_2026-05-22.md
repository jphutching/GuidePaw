# GuidePaw Handover

Generated from the current repository state for a fresh chat handoff.

## Current Snapshot

- Local laptop git HEAD: `2dd8531`
- `origin/main`: `2dd8531`
- Render live deploy: `2dd8531`
- Working tree: clean
- Live companion release: `v0.016` / version code `16`
- Live APK: `https://guidepaw.app/downloads/GuidePaw_Companion_v0.016.apk`

## What Was Finished

- Android companion now uses a bottom navigation bar that matches the web app structure and labels.
- The top `Feedback` and `Menu` buttons were removed from the Android home screen.
- GuidePaw pages opened from the Android app stay inside an in-app WebView instead of forcing the browser.
- A token bridge was added so the Android companion can hand off into the website as a logged-in session.
- Feedback report `#73` was fixed and marked `fixed` in the live database.
- `AGENTS.md` was added as a short repo operating guide and committed separately.

## Android Companion State

- Package: `com.guidepaw.companion`
- Current source version: `0.016`
- Current build outputs are committed under `downloads/`
- The companion release endpoint now serves:
  - `version_name=0.016`
  - `version_code=16`
  - `apk_file=GuidePaw_Companion_v0.016.apk`

## Repo Structure Notes

- Root `*.php` files are page controllers.
- `includes/` holds shared helpers for auth, DB access, SEO, feedback, training, and navigation.
- `api/` holds JSON endpoints used by the Android app.
- `android/guidepaw-companion/` is the native companion app.
- `scripts/` contains deploy, crawl, and release validation utilities.

## Memory Anchors

- The user wants verified current state, not guesses.
- The user prefers durable handoff artifacts when changing phases.
- The Android companion should feel familiar to the web app, not like a wrapper that throws the user into the browser.
- Remove redundant chrome when it duplicates the primary navigation.
- Treat version bumps as a bundle: Android `versionCode`, `versionName`, `CompanionAppVersion`, APK filename, and release endpoint should move together.
- Validate with the local crawler and the live Render/API state before calling a release done.

## Verification That Matters

- `php -l` passes on touched PHP files.
- `./gradlew :app:assembleDebug` passes for the Android companion.
- The local QA crawler passed on the current snapshot.
- Live `api/app_release.php` returns the current Android release metadata.
- Live APK download responds with HTTP `200`.

## Useful Next Chat Prompt

Start from the latest verified state, then continue with the next user-facing Android or web task without redoing the sync checks unless the user asks for a fresh verification pass.
