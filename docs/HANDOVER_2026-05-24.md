# GuidePaw Handover

Generated from the current repository state for a fresh chat handoff.

## Current Snapshot

- Local laptop git HEAD: `0d350ca`
- `origin/main`: `0d350ca`
- Working tree: clean
- Live companion release: `v0.020` / version code `20`
- Live APK: `https://guidepaw.app/downloads/GuidePaw_Companion_v0.020.apk`

## What Was Finished This Session

### CLAUDE.md
- Added a proper `CLAUDE.md` to the repo with architecture guidance, corrected Android source path (`java/` not `kotlin/`), documented `insertAndGetId()`, CSRF helpers, notifications, ZeptoMail email, and Stripe integration.

### Landing page (index.php)
- Merged the two overlapping public-content sections ("Examples" and "Public guides") into one unified "Public tools & guides / Browse without an account" section.
- Fixed three previously unlinked cards: Breed Questionnaire → `breed_questionnaire.php`, Support Options → `support_funding.php`, Service Dog Notes → `service_dog_rights.php`.
- Removed the duplicate Housing & Access FAQ card. Net: −17 lines.

### Android companion v0.020
- Ported the Menu tab from a View-based `MaterialAlertDialogBuilder` (LinearLayout, GridLayout, MaterialCardView — ~175 lines of hybrid code) to a native Compose `ModalBottomSheet`.
- `MainActivity.kt` is now 100% Compose — no more hybrid View/Compose architecture.
- Same menu sections preserved: Dog, Logs, Training, Care, More.
- All four version files bumped together: `build.gradle`, `CompanionAppVersion.kt`, `companion_release.php` defaults, APK in `downloads/`.
- App release endpoint verified: returns `version_code: 20`, correct APK filename and release notes.

## Android Companion State

- Package: `com.guidepaw.companion`
- Current source version: `0.020` / version code `20`
- APK committed to `downloads/GuidePaw_Companion_v0.020.apk`
- Release endpoint (`/api/app_release.php`) serving correct v0.020 metadata

## Verification That Matters

- `php -l` passes on all touched PHP files.
- `./gradlew :app:assembleDebug` passes cleanly (38s).
- `deploy_local.sh` smoke checks pass — PHP syntax, brand header, missing links, HTTP 200/302s all clean.
- `curl -k https://10.147.18.184/api/app_release.php` returns `version_code: 20`.

## Repo Structure Notes

- Root `*.php` files are page controllers.
- `includes/` holds shared helpers; `api/` holds JSON endpoints for the Android app.
- `android/guidepaw-companion/app/src/main/java/com/guidepaw/companion/` — Kotlin source (note: `java/` directory, not `kotlin/`).
- `scripts/deploy_local.sh` — run after every PHP change; enforces brand header and checks for broken links.

## Memory Anchors

- Version bumps are a bundle of four: `build.gradle`, `CompanionAppVersion.kt`, `companion_release.php` defaults, APK in `downloads/`. All must move together.
- The Android app is pure Compose as of v0.020 — do not reintroduce View-based hybrid code into `MainActivity.kt`.
- `deploy_local.sh` must pass before any commit touching PHP.
- `insertAndGetId($pdo, $sql, $params)` from `db_core.php` is the correct way to INSERT and get the new row ID — never `lastInsertId()`.
- Feature flags default to **enabled** for unknown keys (fail-open). New flags need a migration in `sql/migrations/pgsql/`.

## Possible Next Tasks

- Android: Port `FeedbackActivity.kt` from View-based XML layout to Compose (it is currently the last fully View-based activity).
- Android: Add a proper "More" section screen in Compose rather than sending all More items through the bottom sheet only.
- Web: Continue landing page improvements — the "How it works" section and FAQ card row could be reviewed for freshness.
- Web: Review `app.php` (the companion app page) for updated v0.020 release notes.
