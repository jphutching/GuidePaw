function buildHandoffDoc({ from_ai, to_ai, summary, files_changed = [], next_task, branch, session_id, reason }) {
  const ts = new Date().toISOString();
  const fromLabel = (from_ai || "UNKNOWN").toUpperCase();
  const toLabel = (to_ai || "UNKNOWN").toUpperCase();
  const reasonBlock = reason ? `\n> ⚠️ **Handoff reason:** \`${reason}\`\n` : "";
  const filesBlock = files_changed.length > 0
    ? files_changed.map(f => `- \`${f}\``).join("\n")
    : "- *(run `git diff --name-only HEAD~1` to see recent changes)*";

  return `# 🤝 GuidePaw AI Handoff Document
${reasonBlock}
| Field | Value |
|---|---|
| **From** | ${fromLabel} |
| **To** | ${toLabel} |
| **Branch** | \`${branch || "main"}\` |
| **Session ID** | \`${session_id || "unknown"}\` |
| **Timestamp** | \`${ts}\` |

---

## 📋 Summary of Work Completed

${summary || "No summary — check git log for recent commits."}

---

## 📁 Files Changed This Session

${filesBlock}

---

## 🎯 Next Task for ${toLabel}

${next_task || "Review git log and open GitHub issues."}

---

## 🚀 Pickup Instructions for ${toLabel}

\`\`\`bash
# 1. Pull latest (includes this HANDOFF.md)
git pull origin ${branch || "main"}

# 2. Register your session
curl -s -X POST $MIDDLEWARE_URL/session/start \\
  -H "Authorization: Bearer $MIDDLEWARE_SECRET" \\
  -H "Content-Type: application/json" \\
  -d '{"ai":"${to_ai || "NEXT_AI"}","task":"${(next_task || "").replace(/"/g, '\\"')}","branch":"${branch || "main"}"}'

# 3. Check state
curl -s $MIDDLEWARE_URL/status | python3 -m json.tool
\`\`\`

---

## 📌 Middleware Quick Reference

| Action | Command |
|--------|---------|
| Mark milestone | \`curl -X POST $MIDDLEWARE_URL/milestone -H "Authorization: Bearer $MIDDLEWARE_SECRET" -H "Content-Type: application/json" -d '{"ai":"${to_ai}","title":"TITLE","files_changed":["file"]}'\` |
| Token warning | \`curl -X POST $MIDDLEWARE_URL/token-warning -H "Authorization: Bearer $MIDDLEWARE_SECRET" -H "Content-Type: application/json" -d '{"ai":"${to_ai}","tokens_used":N,"last_completed_task":"TASK"}'\` |
| End session | \`curl -X POST $MIDDLEWARE_URL/session/end -H "Authorization: Bearer $MIDDLEWARE_SECRET" -H "Content-Type: application/json" -d '{"ai":"${to_ai}","summary":"SUMMARY","next_task":"TASK"}'\` |

---

## 🗂 Project Context

- **App:** GuidePaw (guidepaw.app) — assistive navigation + Android companion
- **Repo:** https://github.com/jphutching/GuidePaw (private)
- **Local:** /home/james/projects/guidepaw
- **Middleware (laptop):** http://10.147.18.184:3333
- **App (Render):** https://guidepaw-ch3y.onrender.com
- **Middleware (Render):** https://guidepaw-middleware.onrender.com

---
*Auto-generated by GuidePaw Middleware. Do not edit — will be overwritten on next handoff.*
`;
}

module.exports = { buildHandoffDoc };
