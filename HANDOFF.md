# 🤝 GuidePaw AI Handoff Document

> ⚠️ **Handoff reason:** `initial_setup`

| Field | Value |
|---|---|
| **From** | HUMAN (initial setup — claude.ai chat) |
| **To** | CLAUDE CODE CLI |
| **Branch** | `main` |
| **Session ID** | `init-001` |
| **Timestamp** | `2026-05-24T00:00:00.000Z` |

---

## 📋 Summary of Work Completed

The GuidePaw AI middleware system was designed and built in a claude.ai chat session.
All middleware files are ready. The repo needs to be migrated from `gpb3/gpb3` to `projects/guidepaw`.

---

## 📁 Files Added

- `middleware/server.js` — main Express daemon (port 3333)
- `middleware/git-sync.js` — auto-commits HANDOFF.md to GitHub
- `middleware/handoff-template.js` — builds this document
- `middleware/package.json`
- `.claude/CLAUDE.md` — your system context (auto-read by Claude Code)
- `.codex/system_prompt.md` — Codex system context
- `SESSION_STATE.json` — session state
- `HANDOFF.md` — this file
- `render.yaml` — Render Blueprint config
- `scripts/migrate-repo.sh` — moves gpb3/gpb3 → projects/guidepaw
- `scripts/install.sh` — one-command laptop setup
- `scripts/start-claude.sh` — start your session
- `scripts/start-codex.sh` — start Codex session

---

## 🎯 Next Task for CLAUDE CODE

1. Confirm repo path: is it `/home/james/projects/gpb3/gpb3`? If so, run `migrate-repo.sh`
2. Run `scripts/install.sh` to set up the middleware daemon
3. Fill in `middleware/.env` — only 3 secrets need real values:
   - `MIDDLEWARE_SECRET` (auto-generated by install.sh)
   - `ANTHROPIC_API_KEY`
   - `OPENAI_API_KEY`
4. Verify: `curl http://10.147.18.184:3333/health`
5. Review open GitHub issues and pick up highest-priority task

---

## 🚀 Pickup Instructions for CLAUDE CODE

```bash
# On the laptop via SSH from your Android:
ssh james@10.147.18.184

# If repo is still at old path:
ls ~/projects/gpb3/gpb3   # confirm it exists
bash ~/projects/gpb3/gpb3/scripts/migrate-repo.sh

# Then:
cd ~/projects/guidepaw
bash scripts/install.sh

# Fill in secrets:
nano middleware/.env

# Start Claude Code:
bash scripts/start-claude.sh
```

---

*Auto-generated by GuidePaw Middleware — claude.ai session 2026-05-24*
